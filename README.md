### This is the Pytorch implementation of the EEGNet for the [original Keras version](https://github.com/vlawhern/arl-eegmodels).

### 
Dependencies include `PyTorch` and `mne`. Simply install them using `conda` or `pip`.
